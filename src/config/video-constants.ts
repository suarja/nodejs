import { VIDEO_DURATION_MULTIPLIER } from "editia-core";

export const VIDEO_DURATION_FACTOR = VIDEO_DURATION_MULTIPLIER;

// Safety margin for duration validation (5% buffer)
export const SAFETY_MARGIN = 0.95;